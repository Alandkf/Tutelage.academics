export const metadata = {
  title: "Tutelage Tests | Tutelage",
  description: "Take our English proficiency tests",
};

export default function TutelageTestsLayout({ children }) {
  return children;
}
